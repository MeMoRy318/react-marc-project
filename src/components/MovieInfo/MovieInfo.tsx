import React, { FC, PropsWithChildren } from 'react';

type IProps = PropsWithChildren

const MovieInfo: FC<IProps> = () => {
    return (
        <div>

        </div>
    );
};

export { MovieInfo };
